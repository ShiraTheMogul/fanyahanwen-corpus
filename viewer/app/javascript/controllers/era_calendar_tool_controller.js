import { Controller } from "@hotwired/stimulus"

// Compatibility controller only. The era calendar is a normal server-rendered
// Tools panel and is managed by tools_picker_controller like the other tools.
export default class extends Controller {}
