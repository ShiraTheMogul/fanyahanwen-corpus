import { Controller } from "@hotwired/stimulus";

// Placeholder controller for the Textbook page.
// Kept intentionally minimal so the page can attach other block controllers.
export default class extends Controller {}
