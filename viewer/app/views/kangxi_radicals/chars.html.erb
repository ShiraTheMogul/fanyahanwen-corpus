<turbo-frame id="radical_chars">
  <%= render "characters_table",
    radical: @radical,
    characters: @characters,
    defs: @defs,
    def_label: @def_label,
    total: @total,
    page: @page,
    per: @per
  %>
</turbo-frame>
