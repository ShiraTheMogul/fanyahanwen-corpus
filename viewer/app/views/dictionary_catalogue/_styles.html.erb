<style>
  .dictionary-catalogue-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
    gap: 10px;
    margin-top: 14px;
  }

  .dictionary-catalogue-card,
  .dictionary-section-card {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    padding: 12px;
    border: 1px solid rgba(0, 0, 0, 0.18);
    border-radius: 12px;
    text-decoration: none;
    min-height: 92px;
  }

  .dictionary-catalogue-card:hover,
  .dictionary-section-card:hover {
    border-color: rgba(0, 0, 0, 0.35);
  }

  .dictionary-card-title {
    font-size: 1.25rem;
    line-height: 1.2;
  }

  .dictionary-card-meta {
    margin-top: 8px;
    font-size: 0.86rem;
    opacity: 0.7;
    font-variant-numeric: tabular-nums;
  }

  .dictionary-browser-columns {
    display: flex;
    gap: 24px;
    align-items: flex-start;
    flex-wrap: wrap;
  }

  .dictionary-browser-sidebar {
    min-width: 280px;
    max-width: 420px;
  }

  .dictionary-browser-main {
    flex: 1 1 520px;
    min-width: 360px;
  }

  .dictionary-section-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(145px, 1fr));
    gap: 10px;
  }

  .dictionary-section-label {
    font-size: 1.35rem;
    line-height: 1.15;
  }

  .dictionary-status-box {
    padding: 10px 12px;
    border: 1px solid rgba(0, 0, 0, 0.18);
    border-radius: 10px;
    background: rgba(255, 240, 200, 0.35);
    margin: 10px 0 14px;
  }

  .dictionary-stat-list {
    margin: 0;
  }

  .dictionary-stat-list dt {
    font-weight: 700;
    margin-top: 8px;
  }

  .dictionary-stat-list dd {
    margin: 2px 0 0;
  }

  .dictionary-lookup-results {
    margin-top: 14px;
    padding: 10px 12px;
    border: 1px solid rgba(0, 0, 0, 0.18);
    border-radius: 10px;
  }

  .dictionary-entry-table {
    width: 100%;
    border-collapse: collapse;
  }

  .dictionary-entry-table th {
    text-align: left;
    padding: 6px 8px;
    border-bottom: 1px solid rgba(0, 0, 0, 0.15);
  }

  .dictionary-entry-table td {
    padding: 9px 8px;
    border-bottom: 1px solid rgba(0, 0, 0, 0.07);
    vertical-align: top;
  }

  .dictionary-entry-headword {
    font-size: 2rem;
    line-height: 1.1;
  }

  .dictionary-entry-sequence {
    margin-top: 4px;
    font-size: 0.82rem;
    opacity: 0.65;
    font-variant-numeric: tabular-nums;
  }

  .dictionary-group-head {
    display: inline-block;
    margin-top: 4px;
    padding: 1px 6px;
    border: 1px solid rgba(0, 0, 0, 0.18);
    border-radius: 999px;
    font-size: 0.75rem;
    opacity: 0.8;
  }

  .dictionary-pager {
    display: flex;
    gap: 12px;
    align-items: center;
    flex-wrap: wrap;
    margin: 6px 0 12px;
  }

  .dictionary-entry-navigation {
    display: flex;
    justify-content: space-between;
    gap: 16px;
    flex-wrap: wrap;
    margin: 18px 0;
  }

  .dictionary-source-reference {
    padding: 9px 0;
    border-bottom: 1px solid rgba(0, 0, 0, 0.08);
  }

  @media (max-width: 720px) {
    .dictionary-browser-main,
    .dictionary-browser-sidebar {
      min-width: 100%;
      max-width: none;
    }

    .dictionary-entry-table th:nth-child(3),
    .dictionary-entry-table td:nth-child(3) {
      display: none;
    }
  }
</style>
