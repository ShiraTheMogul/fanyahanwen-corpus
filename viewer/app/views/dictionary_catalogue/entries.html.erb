<turbo-frame id="dictionary_section_entries">
  <%= render "entries_table",
    work: @work,
    section: @section,
    entries: @entries,
    total: @total,
    page: @page,
    per: @per %>
</turbo-frame>
