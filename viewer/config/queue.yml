default: &default
  dispatchers:
    - polling_interval: 2
      batch_size: 100
      concurrency_maintenance_interval: 600
  workers:
    - queues: "*"
      threads: <%= ENV.fetch("JOB_THREADS", 1) %>
      processes: 1
      polling_interval: 1

production:
  <<: *default
